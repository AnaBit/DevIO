/*
 * Generated by gdbus-codegen 2.50.0. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "ltebus.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface dev.middleware.lte
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:LteBusDevMiddlewareLte
 * @title: LteBusDevMiddlewareLte
 * @short_description: Generated C code for the dev.middleware.lte D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link> D-Bus interface in C.
 */

/* ---- Introspection data for dev.middleware.lte ---- */

static const _ExtendedGDBusArgInfo _lte_bus_dev_middleware_lte_method_info_mobile_base_station_time_OUT_ARG_time =
{
  {
    -1,
    (gchar *) "time",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _lte_bus_dev_middleware_lte_method_info_mobile_base_station_time_OUT_ARG_pointers[] =
{
  &_lte_bus_dev_middleware_lte_method_info_mobile_base_station_time_OUT_ARG_time,
  NULL
};

static const _ExtendedGDBusMethodInfo _lte_bus_dev_middleware_lte_method_info_mobile_base_station_time =
{
  {
    -1,
    (gchar *) "MobileBaseStationTime",
    NULL,
    (GDBusArgInfo **) &_lte_bus_dev_middleware_lte_method_info_mobile_base_station_time_OUT_ARG_pointers,
    NULL
  },
  "handle-mobile-base-station-time",
  FALSE
};

static const _ExtendedGDBusArgInfo _lte_bus_dev_middleware_lte_method_info_dial_IN_ARG_number =
{
  {
    -1,
    (gchar *) "number",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _lte_bus_dev_middleware_lte_method_info_dial_IN_ARG_pointers[] =
{
  &_lte_bus_dev_middleware_lte_method_info_dial_IN_ARG_number,
  NULL
};

static const _ExtendedGDBusArgInfo _lte_bus_dev_middleware_lte_method_info_dial_OUT_ARG_res =
{
  {
    -1,
    (gchar *) "res",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _lte_bus_dev_middleware_lte_method_info_dial_OUT_ARG_pointers[] =
{
  &_lte_bus_dev_middleware_lte_method_info_dial_OUT_ARG_res,
  NULL
};

static const _ExtendedGDBusMethodInfo _lte_bus_dev_middleware_lte_method_info_dial =
{
  {
    -1,
    (gchar *) "Dial",
    (GDBusArgInfo **) &_lte_bus_dev_middleware_lte_method_info_dial_IN_ARG_pointers,
    (GDBusArgInfo **) &_lte_bus_dev_middleware_lte_method_info_dial_OUT_ARG_pointers,
    NULL
  },
  "handle-dial",
  FALSE
};

static const _ExtendedGDBusMethodInfo _lte_bus_dev_middleware_lte_method_info_handup =
{
  {
    -1,
    (gchar *) "Handup",
    NULL,
    NULL,
    NULL
  },
  "handle-handup",
  FALSE
};

static const _ExtendedGDBusArgInfo _lte_bus_dev_middleware_lte_method_info_open_cellular_data_OUT_ARG_res =
{
  {
    -1,
    (gchar *) "res",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _lte_bus_dev_middleware_lte_method_info_open_cellular_data_OUT_ARG_pointers[] =
{
  &_lte_bus_dev_middleware_lte_method_info_open_cellular_data_OUT_ARG_res,
  NULL
};

static const _ExtendedGDBusMethodInfo _lte_bus_dev_middleware_lte_method_info_open_cellular_data =
{
  {
    -1,
    (gchar *) "OpenCellularData",
    NULL,
    (GDBusArgInfo **) &_lte_bus_dev_middleware_lte_method_info_open_cellular_data_OUT_ARG_pointers,
    NULL
  },
  "handle-open-cellular-data",
  FALSE
};

static const _ExtendedGDBusMethodInfo _lte_bus_dev_middleware_lte_method_info_close_cellular_data =
{
  {
    -1,
    (gchar *) "CloseCellularData",
    NULL,
    NULL,
    NULL
  },
  "handle-close-cellular-data",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _lte_bus_dev_middleware_lte_method_info_pointers[] =
{
  &_lte_bus_dev_middleware_lte_method_info_mobile_base_station_time,
  &_lte_bus_dev_middleware_lte_method_info_dial,
  &_lte_bus_dev_middleware_lte_method_info_handup,
  &_lte_bus_dev_middleware_lte_method_info_open_cellular_data,
  &_lte_bus_dev_middleware_lte_method_info_close_cellular_data,
  NULL
};

static const _ExtendedGDBusArgInfo _lte_bus_dev_middleware_lte_signal_info_dial_stauts_signal_ARG_dial_status =
{
  {
    -1,
    (gchar *) "dial_status",
    (gchar *) "y",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _lte_bus_dev_middleware_lte_signal_info_dial_stauts_signal_ARG_dial_number =
{
  {
    -1,
    (gchar *) "dial_number",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _lte_bus_dev_middleware_lte_signal_info_dial_stauts_signal_ARG_pointers[] =
{
  &_lte_bus_dev_middleware_lte_signal_info_dial_stauts_signal_ARG_dial_status,
  &_lte_bus_dev_middleware_lte_signal_info_dial_stauts_signal_ARG_dial_number,
  NULL
};

static const _ExtendedGDBusSignalInfo _lte_bus_dev_middleware_lte_signal_info_dial_stauts_signal =
{
  {
    -1,
    (gchar *) "DialStautsSignal",
    (GDBusArgInfo **) &_lte_bus_dev_middleware_lte_signal_info_dial_stauts_signal_ARG_pointers,
    NULL
  },
  "dial-stauts-signal"
};

static const _ExtendedGDBusArgInfo _lte_bus_dev_middleware_lte_signal_info_cellular_status_signal_ARG_cellular_status =
{
  {
    -1,
    (gchar *) "cellular_status",
    (gchar *) "y",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _lte_bus_dev_middleware_lte_signal_info_cellular_status_signal_ARG_pointers[] =
{
  &_lte_bus_dev_middleware_lte_signal_info_cellular_status_signal_ARG_cellular_status,
  NULL
};

static const _ExtendedGDBusSignalInfo _lte_bus_dev_middleware_lte_signal_info_cellular_status_signal =
{
  {
    -1,
    (gchar *) "CellularStatusSignal",
    (GDBusArgInfo **) &_lte_bus_dev_middleware_lte_signal_info_cellular_status_signal_ARG_pointers,
    NULL
  },
  "cellular-status-signal"
};

static const _ExtendedGDBusSignalInfo * const _lte_bus_dev_middleware_lte_signal_info_pointers[] =
{
  &_lte_bus_dev_middleware_lte_signal_info_dial_stauts_signal,
  &_lte_bus_dev_middleware_lte_signal_info_cellular_status_signal,
  NULL
};

static const _ExtendedGDBusPropertyInfo _lte_bus_dev_middleware_lte_property_info_build_time =
{
  {
    -1,
    (gchar *) "BuildTime",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "build-time",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _lte_bus_dev_middleware_lte_property_info_verison =
{
  {
    -1,
    (gchar *) "verison",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "verison",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _lte_bus_dev_middleware_lte_property_info_hardware_model =
{
  {
    -1,
    (gchar *) "HardwareModel",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "hardware-model",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _lte_bus_dev_middleware_lte_property_info_iccid =
{
  {
    -1,
    (gchar *) "ICCID",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "iccid",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _lte_bus_dev_middleware_lte_property_info_phone_num =
{
  {
    -1,
    (gchar *) "PhoneNum",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "phone-num",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _lte_bus_dev_middleware_lte_property_info_dial_status =
{
  {
    -1,
    (gchar *) "DialStatus",
    (gchar *) "y",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "dial-status",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _lte_bus_dev_middleware_lte_property_info_cellular_status =
{
  {
    -1,
    (gchar *) "CellularStatus",
    (gchar *) "y",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "cellular-status",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _lte_bus_dev_middleware_lte_property_info_pointers[] =
{
  &_lte_bus_dev_middleware_lte_property_info_build_time,
  &_lte_bus_dev_middleware_lte_property_info_verison,
  &_lte_bus_dev_middleware_lte_property_info_hardware_model,
  &_lte_bus_dev_middleware_lte_property_info_iccid,
  &_lte_bus_dev_middleware_lte_property_info_phone_num,
  &_lte_bus_dev_middleware_lte_property_info_dial_status,
  &_lte_bus_dev_middleware_lte_property_info_cellular_status,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _lte_bus_dev_middleware_lte_interface_info =
{
  {
    -1,
    (gchar *) "dev.middleware.lte",
    (GDBusMethodInfo **) &_lte_bus_dev_middleware_lte_method_info_pointers,
    (GDBusSignalInfo **) &_lte_bus_dev_middleware_lte_signal_info_pointers,
    (GDBusPropertyInfo **) &_lte_bus_dev_middleware_lte_property_info_pointers,
    NULL
  },
  "dev-middleware-lte",
};


/**
 * lte_bus_dev_middleware_lte_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
lte_bus_dev_middleware_lte_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_lte_bus_dev_middleware_lte_interface_info.parent_struct;
}

/**
 * lte_bus_dev_middleware_lte_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #LteBusDevMiddlewareLte interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
lte_bus_dev_middleware_lte_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "build-time");
  g_object_class_override_property (klass, property_id_begin++, "verison");
  g_object_class_override_property (klass, property_id_begin++, "hardware-model");
  g_object_class_override_property (klass, property_id_begin++, "iccid");
  g_object_class_override_property (klass, property_id_begin++, "phone-num");
  g_object_class_override_property (klass, property_id_begin++, "dial-status");
  g_object_class_override_property (klass, property_id_begin++, "cellular-status");
  return property_id_begin - 1;
}



/**
 * LteBusDevMiddlewareLte:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link>.
 */

/**
 * LteBusDevMiddlewareLteIface:
 * @parent_iface: The parent interface.
 * @handle_close_cellular_data: Handler for the #LteBusDevMiddlewareLte::handle-close-cellular-data signal.
 * @handle_dial: Handler for the #LteBusDevMiddlewareLte::handle-dial signal.
 * @handle_handup: Handler for the #LteBusDevMiddlewareLte::handle-handup signal.
 * @handle_mobile_base_station_time: Handler for the #LteBusDevMiddlewareLte::handle-mobile-base-station-time signal.
 * @handle_open_cellular_data: Handler for the #LteBusDevMiddlewareLte::handle-open-cellular-data signal.
 * @get_build_time: Getter for the #LteBusDevMiddlewareLte:build-time property.
 * @get_cellular_status: Getter for the #LteBusDevMiddlewareLte:cellular-status property.
 * @get_dial_status: Getter for the #LteBusDevMiddlewareLte:dial-status property.
 * @get_hardware_model: Getter for the #LteBusDevMiddlewareLte:hardware-model property.
 * @get_iccid: Getter for the #LteBusDevMiddlewareLte:iccid property.
 * @get_phone_num: Getter for the #LteBusDevMiddlewareLte:phone-num property.
 * @get_verison: Getter for the #LteBusDevMiddlewareLte:verison property.
 * @cellular_status_signal: Handler for the #LteBusDevMiddlewareLte::cellular-status-signal signal.
 * @dial_stauts_signal: Handler for the #LteBusDevMiddlewareLte::dial-stauts-signal signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link>.
 */

typedef LteBusDevMiddlewareLteIface LteBusDevMiddlewareLteInterface;
G_DEFINE_INTERFACE (LteBusDevMiddlewareLte, lte_bus_dev_middleware_lte, G_TYPE_OBJECT);

static void
lte_bus_dev_middleware_lte_default_init (LteBusDevMiddlewareLteIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * LteBusDevMiddlewareLte::handle-mobile-base-station-time:
   * @object: A #LteBusDevMiddlewareLte.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-dev-middleware-lte.MobileBaseStationTime">MobileBaseStationTime()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call lte_bus_dev_middleware_lte_complete_mobile_base_station_time() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-mobile-base-station-time",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (LteBusDevMiddlewareLteIface, handle_mobile_base_station_time),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * LteBusDevMiddlewareLte::handle-dial:
   * @object: A #LteBusDevMiddlewareLte.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_number: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-dev-middleware-lte.Dial">Dial()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call lte_bus_dev_middleware_lte_complete_dial() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-dial",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (LteBusDevMiddlewareLteIface, handle_dial),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * LteBusDevMiddlewareLte::handle-handup:
   * @object: A #LteBusDevMiddlewareLte.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-dev-middleware-lte.Handup">Handup()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call lte_bus_dev_middleware_lte_complete_handup() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-handup",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (LteBusDevMiddlewareLteIface, handle_handup),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * LteBusDevMiddlewareLte::handle-open-cellular-data:
   * @object: A #LteBusDevMiddlewareLte.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-dev-middleware-lte.OpenCellularData">OpenCellularData()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call lte_bus_dev_middleware_lte_complete_open_cellular_data() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-open-cellular-data",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (LteBusDevMiddlewareLteIface, handle_open_cellular_data),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * LteBusDevMiddlewareLte::handle-close-cellular-data:
   * @object: A #LteBusDevMiddlewareLte.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-dev-middleware-lte.CloseCellularData">CloseCellularData()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call lte_bus_dev_middleware_lte_complete_close_cellular_data() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-close-cellular-data",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (LteBusDevMiddlewareLteIface, handle_close_cellular_data),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * LteBusDevMiddlewareLte::dial-stauts-signal:
   * @object: A #LteBusDevMiddlewareLte.
   * @arg_dial_status: Argument.
   * @arg_dial_number: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-dev-middleware-lte.DialStautsSignal">"DialStautsSignal"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("dial-stauts-signal",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (LteBusDevMiddlewareLteIface, dial_stauts_signal),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_UCHAR, G_TYPE_STRING);

  /**
   * LteBusDevMiddlewareLte::cellular-status-signal:
   * @object: A #LteBusDevMiddlewareLte.
   * @arg_cellular_status: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-dev-middleware-lte.CellularStatusSignal">"CellularStatusSignal"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("cellular-status-signal",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (LteBusDevMiddlewareLteIface, cellular_status_signal),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_UCHAR);

  /* GObject properties for D-Bus properties: */
  /**
   * LteBusDevMiddlewareLte:build-time:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-dev-middleware-lte.BuildTime">"BuildTime"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("build-time", "BuildTime", "BuildTime", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * LteBusDevMiddlewareLte:verison:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-dev-middleware-lte.verison">"verison"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("verison", "verison", "verison", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * LteBusDevMiddlewareLte:hardware-model:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-dev-middleware-lte.HardwareModel">"HardwareModel"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("hardware-model", "HardwareModel", "HardwareModel", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * LteBusDevMiddlewareLte:iccid:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-dev-middleware-lte.ICCID">"ICCID"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("iccid", "ICCID", "ICCID", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * LteBusDevMiddlewareLte:phone-num:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-dev-middleware-lte.PhoneNum">"PhoneNum"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("phone-num", "PhoneNum", "PhoneNum", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * LteBusDevMiddlewareLte:dial-status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-dev-middleware-lte.DialStatus">"DialStatus"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uchar ("dial-status", "DialStatus", "DialStatus", 0, 255, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * LteBusDevMiddlewareLte:cellular-status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-dev-middleware-lte.CellularStatus">"CellularStatus"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uchar ("cellular-status", "CellularStatus", "CellularStatus", 0, 255, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * lte_bus_dev_middleware_lte_get_build_time: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets the value of the <link linkend="gdbus-property-dev-middleware-lte.BuildTime">"BuildTime"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use lte_bus_dev_middleware_lte_dup_build_time() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
lte_bus_dev_middleware_lte_get_build_time (LteBusDevMiddlewareLte *object)
{
  return LTE_BUS_DEV_MIDDLEWARE_LTE_GET_IFACE (object)->get_build_time (object);
}

/**
 * lte_bus_dev_middleware_lte_dup_build_time: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets a copy of the <link linkend="gdbus-property-dev-middleware-lte.BuildTime">"BuildTime"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
lte_bus_dev_middleware_lte_dup_build_time (LteBusDevMiddlewareLte *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "build-time", &value, NULL);
  return value;
}

/**
 * lte_bus_dev_middleware_lte_set_build_time: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-dev-middleware-lte.BuildTime">"BuildTime"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
lte_bus_dev_middleware_lte_set_build_time (LteBusDevMiddlewareLte *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "build-time", value, NULL);
}

/**
 * lte_bus_dev_middleware_lte_get_verison: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets the value of the <link linkend="gdbus-property-dev-middleware-lte.verison">"verison"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use lte_bus_dev_middleware_lte_dup_verison() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
lte_bus_dev_middleware_lte_get_verison (LteBusDevMiddlewareLte *object)
{
  return LTE_BUS_DEV_MIDDLEWARE_LTE_GET_IFACE (object)->get_verison (object);
}

/**
 * lte_bus_dev_middleware_lte_dup_verison: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets a copy of the <link linkend="gdbus-property-dev-middleware-lte.verison">"verison"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
lte_bus_dev_middleware_lte_dup_verison (LteBusDevMiddlewareLte *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "verison", &value, NULL);
  return value;
}

/**
 * lte_bus_dev_middleware_lte_set_verison: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-dev-middleware-lte.verison">"verison"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
lte_bus_dev_middleware_lte_set_verison (LteBusDevMiddlewareLte *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "verison", value, NULL);
}

/**
 * lte_bus_dev_middleware_lte_get_hardware_model: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets the value of the <link linkend="gdbus-property-dev-middleware-lte.HardwareModel">"HardwareModel"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use lte_bus_dev_middleware_lte_dup_hardware_model() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
lte_bus_dev_middleware_lte_get_hardware_model (LteBusDevMiddlewareLte *object)
{
  return LTE_BUS_DEV_MIDDLEWARE_LTE_GET_IFACE (object)->get_hardware_model (object);
}

/**
 * lte_bus_dev_middleware_lte_dup_hardware_model: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets a copy of the <link linkend="gdbus-property-dev-middleware-lte.HardwareModel">"HardwareModel"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
lte_bus_dev_middleware_lte_dup_hardware_model (LteBusDevMiddlewareLte *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "hardware-model", &value, NULL);
  return value;
}

/**
 * lte_bus_dev_middleware_lte_set_hardware_model: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-dev-middleware-lte.HardwareModel">"HardwareModel"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
lte_bus_dev_middleware_lte_set_hardware_model (LteBusDevMiddlewareLte *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "hardware-model", value, NULL);
}

/**
 * lte_bus_dev_middleware_lte_get_iccid: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets the value of the <link linkend="gdbus-property-dev-middleware-lte.ICCID">"ICCID"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use lte_bus_dev_middleware_lte_dup_iccid() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
lte_bus_dev_middleware_lte_get_iccid (LteBusDevMiddlewareLte *object)
{
  return LTE_BUS_DEV_MIDDLEWARE_LTE_GET_IFACE (object)->get_iccid (object);
}

/**
 * lte_bus_dev_middleware_lte_dup_iccid: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets a copy of the <link linkend="gdbus-property-dev-middleware-lte.ICCID">"ICCID"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
lte_bus_dev_middleware_lte_dup_iccid (LteBusDevMiddlewareLte *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "iccid", &value, NULL);
  return value;
}

/**
 * lte_bus_dev_middleware_lte_set_iccid: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-dev-middleware-lte.ICCID">"ICCID"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
lte_bus_dev_middleware_lte_set_iccid (LteBusDevMiddlewareLte *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "iccid", value, NULL);
}

/**
 * lte_bus_dev_middleware_lte_get_phone_num: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets the value of the <link linkend="gdbus-property-dev-middleware-lte.PhoneNum">"PhoneNum"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use lte_bus_dev_middleware_lte_dup_phone_num() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
lte_bus_dev_middleware_lte_get_phone_num (LteBusDevMiddlewareLte *object)
{
  return LTE_BUS_DEV_MIDDLEWARE_LTE_GET_IFACE (object)->get_phone_num (object);
}

/**
 * lte_bus_dev_middleware_lte_dup_phone_num: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets a copy of the <link linkend="gdbus-property-dev-middleware-lte.PhoneNum">"PhoneNum"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
lte_bus_dev_middleware_lte_dup_phone_num (LteBusDevMiddlewareLte *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "phone-num", &value, NULL);
  return value;
}

/**
 * lte_bus_dev_middleware_lte_set_phone_num: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-dev-middleware-lte.PhoneNum">"PhoneNum"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
lte_bus_dev_middleware_lte_set_phone_num (LteBusDevMiddlewareLte *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "phone-num", value, NULL);
}

/**
 * lte_bus_dev_middleware_lte_get_dial_status: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets the value of the <link linkend="gdbus-property-dev-middleware-lte.DialStatus">"DialStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guchar 
lte_bus_dev_middleware_lte_get_dial_status (LteBusDevMiddlewareLte *object)
{
  return LTE_BUS_DEV_MIDDLEWARE_LTE_GET_IFACE (object)->get_dial_status (object);
}

/**
 * lte_bus_dev_middleware_lte_set_dial_status: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-dev-middleware-lte.DialStatus">"DialStatus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
lte_bus_dev_middleware_lte_set_dial_status (LteBusDevMiddlewareLte *object, guchar value)
{
  g_object_set (G_OBJECT (object), "dial-status", value, NULL);
}

/**
 * lte_bus_dev_middleware_lte_get_cellular_status: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 *
 * Gets the value of the <link linkend="gdbus-property-dev-middleware-lte.CellularStatus">"CellularStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guchar 
lte_bus_dev_middleware_lte_get_cellular_status (LteBusDevMiddlewareLte *object)
{
  return LTE_BUS_DEV_MIDDLEWARE_LTE_GET_IFACE (object)->get_cellular_status (object);
}

/**
 * lte_bus_dev_middleware_lte_set_cellular_status: (skip)
 * @object: A #LteBusDevMiddlewareLte.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-dev-middleware-lte.CellularStatus">"CellularStatus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
lte_bus_dev_middleware_lte_set_cellular_status (LteBusDevMiddlewareLte *object, guchar value)
{
  g_object_set (G_OBJECT (object), "cellular-status", value, NULL);
}

/**
 * lte_bus_dev_middleware_lte_emit_dial_stauts_signal:
 * @object: A #LteBusDevMiddlewareLte.
 * @arg_dial_status: Argument to pass with the signal.
 * @arg_dial_number: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-dev-middleware-lte.DialStautsSignal">"DialStautsSignal"</link> D-Bus signal.
 */
void
lte_bus_dev_middleware_lte_emit_dial_stauts_signal (
    LteBusDevMiddlewareLte *object,
    guchar arg_dial_status,
    const gchar *arg_dial_number)
{
  g_signal_emit_by_name (object, "dial-stauts-signal", arg_dial_status, arg_dial_number);
}

/**
 * lte_bus_dev_middleware_lte_emit_cellular_status_signal:
 * @object: A #LteBusDevMiddlewareLte.
 * @arg_cellular_status: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-dev-middleware-lte.CellularStatusSignal">"CellularStatusSignal"</link> D-Bus signal.
 */
void
lte_bus_dev_middleware_lte_emit_cellular_status_signal (
    LteBusDevMiddlewareLte *object,
    guchar arg_cellular_status)
{
  g_signal_emit_by_name (object, "cellular-status-signal", arg_cellular_status);
}

/**
 * lte_bus_dev_middleware_lte_call_mobile_base_station_time:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.MobileBaseStationTime">MobileBaseStationTime()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call lte_bus_dev_middleware_lte_call_mobile_base_station_time_finish() to get the result of the operation.
 *
 * See lte_bus_dev_middleware_lte_call_mobile_base_station_time_sync() for the synchronous, blocking version of this method.
 */
void
lte_bus_dev_middleware_lte_call_mobile_base_station_time (
    LteBusDevMiddlewareLte *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "MobileBaseStationTime",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * lte_bus_dev_middleware_lte_call_mobile_base_station_time_finish:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @out_time: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to lte_bus_dev_middleware_lte_call_mobile_base_station_time().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with lte_bus_dev_middleware_lte_call_mobile_base_station_time().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_mobile_base_station_time_finish (
    LteBusDevMiddlewareLte *proxy,
    gchar **out_time,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_time);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_call_mobile_base_station_time_sync:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @out_time: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.MobileBaseStationTime">MobileBaseStationTime()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See lte_bus_dev_middleware_lte_call_mobile_base_station_time() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_mobile_base_station_time_sync (
    LteBusDevMiddlewareLte *proxy,
    gchar **out_time,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "MobileBaseStationTime",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_time);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_call_dial:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @arg_number: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.Dial">Dial()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call lte_bus_dev_middleware_lte_call_dial_finish() to get the result of the operation.
 *
 * See lte_bus_dev_middleware_lte_call_dial_sync() for the synchronous, blocking version of this method.
 */
void
lte_bus_dev_middleware_lte_call_dial (
    LteBusDevMiddlewareLte *proxy,
    const gchar *arg_number,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Dial",
    g_variant_new ("(s)",
                   arg_number),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * lte_bus_dev_middleware_lte_call_dial_finish:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @out_res: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to lte_bus_dev_middleware_lte_call_dial().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with lte_bus_dev_middleware_lte_call_dial().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_dial_finish (
    LteBusDevMiddlewareLte *proxy,
    gboolean *out_res,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_res);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_call_dial_sync:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @arg_number: Argument to pass with the method invocation.
 * @out_res: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.Dial">Dial()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See lte_bus_dev_middleware_lte_call_dial() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_dial_sync (
    LteBusDevMiddlewareLte *proxy,
    const gchar *arg_number,
    gboolean *out_res,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Dial",
    g_variant_new ("(s)",
                   arg_number),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_res);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_call_handup:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.Handup">Handup()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call lte_bus_dev_middleware_lte_call_handup_finish() to get the result of the operation.
 *
 * See lte_bus_dev_middleware_lte_call_handup_sync() for the synchronous, blocking version of this method.
 */
void
lte_bus_dev_middleware_lte_call_handup (
    LteBusDevMiddlewareLte *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Handup",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * lte_bus_dev_middleware_lte_call_handup_finish:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to lte_bus_dev_middleware_lte_call_handup().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with lte_bus_dev_middleware_lte_call_handup().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_handup_finish (
    LteBusDevMiddlewareLte *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_call_handup_sync:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.Handup">Handup()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See lte_bus_dev_middleware_lte_call_handup() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_handup_sync (
    LteBusDevMiddlewareLte *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Handup",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_call_open_cellular_data:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.OpenCellularData">OpenCellularData()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call lte_bus_dev_middleware_lte_call_open_cellular_data_finish() to get the result of the operation.
 *
 * See lte_bus_dev_middleware_lte_call_open_cellular_data_sync() for the synchronous, blocking version of this method.
 */
void
lte_bus_dev_middleware_lte_call_open_cellular_data (
    LteBusDevMiddlewareLte *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "OpenCellularData",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * lte_bus_dev_middleware_lte_call_open_cellular_data_finish:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @out_res: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to lte_bus_dev_middleware_lte_call_open_cellular_data().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with lte_bus_dev_middleware_lte_call_open_cellular_data().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_open_cellular_data_finish (
    LteBusDevMiddlewareLte *proxy,
    gboolean *out_res,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_res);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_call_open_cellular_data_sync:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @out_res: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.OpenCellularData">OpenCellularData()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See lte_bus_dev_middleware_lte_call_open_cellular_data() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_open_cellular_data_sync (
    LteBusDevMiddlewareLte *proxy,
    gboolean *out_res,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "OpenCellularData",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_res);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_call_close_cellular_data:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.CloseCellularData">CloseCellularData()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call lte_bus_dev_middleware_lte_call_close_cellular_data_finish() to get the result of the operation.
 *
 * See lte_bus_dev_middleware_lte_call_close_cellular_data_sync() for the synchronous, blocking version of this method.
 */
void
lte_bus_dev_middleware_lte_call_close_cellular_data (
    LteBusDevMiddlewareLte *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "CloseCellularData",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * lte_bus_dev_middleware_lte_call_close_cellular_data_finish:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to lte_bus_dev_middleware_lte_call_close_cellular_data().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with lte_bus_dev_middleware_lte_call_close_cellular_data().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_close_cellular_data_finish (
    LteBusDevMiddlewareLte *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_call_close_cellular_data_sync:
 * @proxy: A #LteBusDevMiddlewareLteProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-dev-middleware-lte.CloseCellularData">CloseCellularData()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See lte_bus_dev_middleware_lte_call_close_cellular_data() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
lte_bus_dev_middleware_lte_call_close_cellular_data_sync (
    LteBusDevMiddlewareLte *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "CloseCellularData",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * lte_bus_dev_middleware_lte_complete_mobile_base_station_time:
 * @object: A #LteBusDevMiddlewareLte.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @time: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-dev-middleware-lte.MobileBaseStationTime">MobileBaseStationTime()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
lte_bus_dev_middleware_lte_complete_mobile_base_station_time (
    LteBusDevMiddlewareLte *object,
    GDBusMethodInvocation *invocation,
    const gchar *time)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   time));
}

/**
 * lte_bus_dev_middleware_lte_complete_dial:
 * @object: A #LteBusDevMiddlewareLte.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @res: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-dev-middleware-lte.Dial">Dial()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
lte_bus_dev_middleware_lte_complete_dial (
    LteBusDevMiddlewareLte *object,
    GDBusMethodInvocation *invocation,
    gboolean res)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   res));
}

/**
 * lte_bus_dev_middleware_lte_complete_handup:
 * @object: A #LteBusDevMiddlewareLte.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-dev-middleware-lte.Handup">Handup()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
lte_bus_dev_middleware_lte_complete_handup (
    LteBusDevMiddlewareLte *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * lte_bus_dev_middleware_lte_complete_open_cellular_data:
 * @object: A #LteBusDevMiddlewareLte.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @res: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-dev-middleware-lte.OpenCellularData">OpenCellularData()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
lte_bus_dev_middleware_lte_complete_open_cellular_data (
    LteBusDevMiddlewareLte *object,
    GDBusMethodInvocation *invocation,
    gboolean res)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   res));
}

/**
 * lte_bus_dev_middleware_lte_complete_close_cellular_data:
 * @object: A #LteBusDevMiddlewareLte.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-dev-middleware-lte.CloseCellularData">CloseCellularData()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
lte_bus_dev_middleware_lte_complete_close_cellular_data (
    LteBusDevMiddlewareLte *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * LteBusDevMiddlewareLteProxy:
 *
 * The #LteBusDevMiddlewareLteProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * LteBusDevMiddlewareLteProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #LteBusDevMiddlewareLteProxy.
 */

struct _LteBusDevMiddlewareLteProxyPrivate
{
  GData *qdata;
};

static void lte_bus_dev_middleware_lte_proxy_iface_init (LteBusDevMiddlewareLteIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (LteBusDevMiddlewareLteProxy, lte_bus_dev_middleware_lte_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (LteBusDevMiddlewareLteProxy)
                         G_IMPLEMENT_INTERFACE (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE, lte_bus_dev_middleware_lte_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (LteBusDevMiddlewareLteProxy, lte_bus_dev_middleware_lte_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE, lte_bus_dev_middleware_lte_proxy_iface_init));

#endif
static void
lte_bus_dev_middleware_lte_proxy_finalize (GObject *object)
{
  LteBusDevMiddlewareLteProxy *proxy = LTE_BUS_DEV_MIDDLEWARE_LTE_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (lte_bus_dev_middleware_lte_proxy_parent_class)->finalize (object);
}

static void
lte_bus_dev_middleware_lte_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  info = _lte_bus_dev_middleware_lte_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
lte_bus_dev_middleware_lte_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface dev.middleware.lte: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
lte_bus_dev_middleware_lte_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  info = _lte_bus_dev_middleware_lte_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "dev.middleware.lte", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) lte_bus_dev_middleware_lte_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
lte_bus_dev_middleware_lte_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_lte_bus_dev_middleware_lte_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
lte_bus_dev_middleware_lte_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  LteBusDevMiddlewareLteProxy *proxy = LTE_BUS_DEV_MIDDLEWARE_LTE_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_lte_bus_dev_middleware_lte_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_lte_bus_dev_middleware_lte_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
lte_bus_dev_middleware_lte_proxy_get_build_time (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteProxy *proxy = LTE_BUS_DEV_MIDDLEWARE_LTE_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "BuildTime");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
lte_bus_dev_middleware_lte_proxy_get_verison (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteProxy *proxy = LTE_BUS_DEV_MIDDLEWARE_LTE_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "verison");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
lte_bus_dev_middleware_lte_proxy_get_hardware_model (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteProxy *proxy = LTE_BUS_DEV_MIDDLEWARE_LTE_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "HardwareModel");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
lte_bus_dev_middleware_lte_proxy_get_iccid (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteProxy *proxy = LTE_BUS_DEV_MIDDLEWARE_LTE_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ICCID");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
lte_bus_dev_middleware_lte_proxy_get_phone_num (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteProxy *proxy = LTE_BUS_DEV_MIDDLEWARE_LTE_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PhoneNum");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guchar 
lte_bus_dev_middleware_lte_proxy_get_dial_status (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteProxy *proxy = LTE_BUS_DEV_MIDDLEWARE_LTE_PROXY (object);
  GVariant *variant;
  guchar value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DialStatus");
  if (variant != NULL)
    {
      value = g_variant_get_byte (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guchar 
lte_bus_dev_middleware_lte_proxy_get_cellular_status (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteProxy *proxy = LTE_BUS_DEV_MIDDLEWARE_LTE_PROXY (object);
  GVariant *variant;
  guchar value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CellularStatus");
  if (variant != NULL)
    {
      value = g_variant_get_byte (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
lte_bus_dev_middleware_lte_proxy_init (LteBusDevMiddlewareLteProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = lte_bus_dev_middleware_lte_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE_PROXY, LteBusDevMiddlewareLteProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), lte_bus_dev_middleware_lte_interface_info ());
}

static void
lte_bus_dev_middleware_lte_proxy_class_init (LteBusDevMiddlewareLteProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = lte_bus_dev_middleware_lte_proxy_finalize;
  gobject_class->get_property = lte_bus_dev_middleware_lte_proxy_get_property;
  gobject_class->set_property = lte_bus_dev_middleware_lte_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = lte_bus_dev_middleware_lte_proxy_g_signal;
  proxy_class->g_properties_changed = lte_bus_dev_middleware_lte_proxy_g_properties_changed;

  lte_bus_dev_middleware_lte_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (LteBusDevMiddlewareLteProxyPrivate));
#endif
}

static void
lte_bus_dev_middleware_lte_proxy_iface_init (LteBusDevMiddlewareLteIface *iface)
{
  iface->get_build_time = lte_bus_dev_middleware_lte_proxy_get_build_time;
  iface->get_verison = lte_bus_dev_middleware_lte_proxy_get_verison;
  iface->get_hardware_model = lte_bus_dev_middleware_lte_proxy_get_hardware_model;
  iface->get_iccid = lte_bus_dev_middleware_lte_proxy_get_iccid;
  iface->get_phone_num = lte_bus_dev_middleware_lte_proxy_get_phone_num;
  iface->get_dial_status = lte_bus_dev_middleware_lte_proxy_get_dial_status;
  iface->get_cellular_status = lte_bus_dev_middleware_lte_proxy_get_cellular_status;
}

/**
 * lte_bus_dev_middleware_lte_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call lte_bus_dev_middleware_lte_proxy_new_finish() to get the result of the operation.
 *
 * See lte_bus_dev_middleware_lte_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
lte_bus_dev_middleware_lte_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "dev.middleware.lte", NULL);
}

/**
 * lte_bus_dev_middleware_lte_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to lte_bus_dev_middleware_lte_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with lte_bus_dev_middleware_lte_proxy_new().
 *
 * Returns: (transfer full) (type LteBusDevMiddlewareLteProxy): The constructed proxy object or %NULL if @error is set.
 */
LteBusDevMiddlewareLte *
lte_bus_dev_middleware_lte_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return LTE_BUS_DEV_MIDDLEWARE_LTE (ret);
  else
    return NULL;
}

/**
 * lte_bus_dev_middleware_lte_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See lte_bus_dev_middleware_lte_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type LteBusDevMiddlewareLteProxy): The constructed proxy object or %NULL if @error is set.
 */
LteBusDevMiddlewareLte *
lte_bus_dev_middleware_lte_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "dev.middleware.lte", NULL);
  if (ret != NULL)
    return LTE_BUS_DEV_MIDDLEWARE_LTE (ret);
  else
    return NULL;
}


/**
 * lte_bus_dev_middleware_lte_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like lte_bus_dev_middleware_lte_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call lte_bus_dev_middleware_lte_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See lte_bus_dev_middleware_lte_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
lte_bus_dev_middleware_lte_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "dev.middleware.lte", NULL);
}

/**
 * lte_bus_dev_middleware_lte_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to lte_bus_dev_middleware_lte_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with lte_bus_dev_middleware_lte_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type LteBusDevMiddlewareLteProxy): The constructed proxy object or %NULL if @error is set.
 */
LteBusDevMiddlewareLte *
lte_bus_dev_middleware_lte_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return LTE_BUS_DEV_MIDDLEWARE_LTE (ret);
  else
    return NULL;
}

/**
 * lte_bus_dev_middleware_lte_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like lte_bus_dev_middleware_lte_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See lte_bus_dev_middleware_lte_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type LteBusDevMiddlewareLteProxy): The constructed proxy object or %NULL if @error is set.
 */
LteBusDevMiddlewareLte *
lte_bus_dev_middleware_lte_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "dev.middleware.lte", NULL);
  if (ret != NULL)
    return LTE_BUS_DEV_MIDDLEWARE_LTE (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * LteBusDevMiddlewareLteSkeleton:
 *
 * The #LteBusDevMiddlewareLteSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * LteBusDevMiddlewareLteSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #LteBusDevMiddlewareLteSkeleton.
 */

struct _LteBusDevMiddlewareLteSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_lte_bus_dev_middleware_lte_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_lte_bus_dev_middleware_lte_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_lte_bus_dev_middleware_lte_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_lte_bus_dev_middleware_lte_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_lte_bus_dev_middleware_lte_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _lte_bus_dev_middleware_lte_skeleton_vtable =
{
  _lte_bus_dev_middleware_lte_skeleton_handle_method_call,
  _lte_bus_dev_middleware_lte_skeleton_handle_get_property,
  _lte_bus_dev_middleware_lte_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
lte_bus_dev_middleware_lte_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return lte_bus_dev_middleware_lte_interface_info ();
}

static GDBusInterfaceVTable *
lte_bus_dev_middleware_lte_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_lte_bus_dev_middleware_lte_skeleton_vtable;
}

static GVariant *
lte_bus_dev_middleware_lte_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_lte_bus_dev_middleware_lte_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _lte_bus_dev_middleware_lte_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _lte_bus_dev_middleware_lte_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _lte_bus_dev_middleware_lte_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "dev.middleware.lte", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _lte_bus_dev_middleware_lte_emit_changed (gpointer user_data);

static void
lte_bus_dev_middleware_lte_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _lte_bus_dev_middleware_lte_emit_changed (skeleton);
}

static void
_lte_bus_dev_middleware_lte_on_signal_dial_stauts_signal (
    LteBusDevMiddlewareLte *object,
    guchar arg_dial_status,
    const gchar *arg_dial_number)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ys)",
                   arg_dial_status,
                   arg_dial_number));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "dev.middleware.lte", "DialStautsSignal",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_lte_bus_dev_middleware_lte_on_signal_cellular_status_signal (
    LteBusDevMiddlewareLte *object,
    guchar arg_cellular_status)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(y)",
                   arg_cellular_status));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "dev.middleware.lte", "CellularStatusSignal",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void lte_bus_dev_middleware_lte_skeleton_iface_init (LteBusDevMiddlewareLteIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (LteBusDevMiddlewareLteSkeleton, lte_bus_dev_middleware_lte_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (LteBusDevMiddlewareLteSkeleton)
                         G_IMPLEMENT_INTERFACE (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE, lte_bus_dev_middleware_lte_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (LteBusDevMiddlewareLteSkeleton, lte_bus_dev_middleware_lte_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE, lte_bus_dev_middleware_lte_skeleton_iface_init));

#endif
static void
lte_bus_dev_middleware_lte_skeleton_finalize (GObject *object)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  guint n;
  for (n = 0; n < 7; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (lte_bus_dev_middleware_lte_skeleton_parent_class)->finalize (object);
}

static void
lte_bus_dev_middleware_lte_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_lte_bus_dev_middleware_lte_emit_changed (gpointer user_data)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "dev.middleware.lte",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_lte_bus_dev_middleware_lte_schedule_emit_changed (LteBusDevMiddlewareLteSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
lte_bus_dev_middleware_lte_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _lte_bus_dev_middleware_lte_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _lte_bus_dev_middleware_lte_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
lte_bus_dev_middleware_lte_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _lte_bus_dev_middleware_lte_schedule_emit_changed (skeleton, _lte_bus_dev_middleware_lte_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
lte_bus_dev_middleware_lte_skeleton_init (LteBusDevMiddlewareLteSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = lte_bus_dev_middleware_lte_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE_SKELETON, LteBusDevMiddlewareLteSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 7);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_UCHAR);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_UCHAR);
}

static const gchar *
lte_bus_dev_middleware_lte_skeleton_get_build_time (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
lte_bus_dev_middleware_lte_skeleton_get_verison (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
lte_bus_dev_middleware_lte_skeleton_get_hardware_model (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
lte_bus_dev_middleware_lte_skeleton_get_iccid (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
lte_bus_dev_middleware_lte_skeleton_get_phone_num (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guchar 
lte_bus_dev_middleware_lte_skeleton_get_dial_status (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  guchar value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uchar (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guchar 
lte_bus_dev_middleware_lte_skeleton_get_cellular_status (LteBusDevMiddlewareLte *object)
{
  LteBusDevMiddlewareLteSkeleton *skeleton = LTE_BUS_DEV_MIDDLEWARE_LTE_SKELETON (object);
  guchar value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uchar (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
lte_bus_dev_middleware_lte_skeleton_class_init (LteBusDevMiddlewareLteSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = lte_bus_dev_middleware_lte_skeleton_finalize;
  gobject_class->get_property = lte_bus_dev_middleware_lte_skeleton_get_property;
  gobject_class->set_property = lte_bus_dev_middleware_lte_skeleton_set_property;
  gobject_class->notify       = lte_bus_dev_middleware_lte_skeleton_notify;


  lte_bus_dev_middleware_lte_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = lte_bus_dev_middleware_lte_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = lte_bus_dev_middleware_lte_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = lte_bus_dev_middleware_lte_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = lte_bus_dev_middleware_lte_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (LteBusDevMiddlewareLteSkeletonPrivate));
#endif
}

static void
lte_bus_dev_middleware_lte_skeleton_iface_init (LteBusDevMiddlewareLteIface *iface)
{
  iface->dial_stauts_signal = _lte_bus_dev_middleware_lte_on_signal_dial_stauts_signal;
  iface->cellular_status_signal = _lte_bus_dev_middleware_lte_on_signal_cellular_status_signal;
  iface->get_build_time = lte_bus_dev_middleware_lte_skeleton_get_build_time;
  iface->get_verison = lte_bus_dev_middleware_lte_skeleton_get_verison;
  iface->get_hardware_model = lte_bus_dev_middleware_lte_skeleton_get_hardware_model;
  iface->get_iccid = lte_bus_dev_middleware_lte_skeleton_get_iccid;
  iface->get_phone_num = lte_bus_dev_middleware_lte_skeleton_get_phone_num;
  iface->get_dial_status = lte_bus_dev_middleware_lte_skeleton_get_dial_status;
  iface->get_cellular_status = lte_bus_dev_middleware_lte_skeleton_get_cellular_status;
}

/**
 * lte_bus_dev_middleware_lte_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link>.
 *
 * Returns: (transfer full) (type LteBusDevMiddlewareLteSkeleton): The skeleton object.
 */
LteBusDevMiddlewareLte *
lte_bus_dev_middleware_lte_skeleton_new (void)
{
  return LTE_BUS_DEV_MIDDLEWARE_LTE (g_object_new (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for Object, ObjectProxy and ObjectSkeleton
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:LteBusObject
 * @title: LteBusObject
 * @short_description: Specialized GDBusObject types
 *
 * This section contains the #LteBusObject, #LteBusObjectProxy, and #LteBusObjectSkeleton types which make it easier to work with objects implementing generated types for D-Bus interfaces.
 */

/**
 * LteBusObject:
 *
 * The #LteBusObject type is a specialized container of interfaces.
 */

/**
 * LteBusObjectIface:
 * @parent_iface: The parent interface.
 *
 * Virtual table for the #LteBusObject interface.
 */

typedef LteBusObjectIface LteBusObjectInterface;
G_DEFINE_INTERFACE_WITH_CODE (LteBusObject, lte_bus_object, G_TYPE_OBJECT, g_type_interface_add_prerequisite (g_define_type_id, G_TYPE_DBUS_OBJECT));

static void
lte_bus_object_default_init (LteBusObjectIface *iface)
{
  /**
   * LteBusObject:dev-middleware-lte:
   *
   * The #LteBusDevMiddlewareLte instance corresponding to the D-Bus interface <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("dev-middleware-lte", "dev-middleware-lte", "dev-middleware-lte", LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE, G_PARAM_READWRITE|G_PARAM_STATIC_STRINGS));

}

/**
 * lte_bus_object_get_dev_middleware_lte:
 * @object: A #LteBusObject.
 *
 * Gets the #LteBusDevMiddlewareLte instance for the D-Bus interface <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link> on @object, if any.
 *
 * Returns: (transfer full): A #LteBusDevMiddlewareLte that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
LteBusDevMiddlewareLte *lte_bus_object_get_dev_middleware_lte (LteBusObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "dev.middleware.lte");
  if (ret == NULL)
    return NULL;
  return LTE_BUS_DEV_MIDDLEWARE_LTE (ret);
}


/**
 * lte_bus_object_peek_dev_middleware_lte: (skip)
 * @object: A #LteBusObject.
 *
 * Like lte_bus_object_get_dev_middleware_lte() but doesn't increase the reference count on the returned object.
 *
 * <warning>It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.</warning>
 *
 * Returns: (transfer none): A #LteBusDevMiddlewareLte or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
LteBusDevMiddlewareLte *lte_bus_object_peek_dev_middleware_lte (LteBusObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "dev.middleware.lte");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return LTE_BUS_DEV_MIDDLEWARE_LTE (ret);
}


static void
lte_bus_object_notify (GDBusObject *object, GDBusInterface *interface)
{
  _ExtendedGDBusInterfaceInfo *info = (_ExtendedGDBusInterfaceInfo *) g_dbus_interface_get_info (interface);
  /* info can be NULL if the other end is using a D-Bus interface we don't know
   * anything about, for example old generated code in this process talking to
   * newer generated code in the other process. */
  if (info != NULL)
    g_object_notify (G_OBJECT (object), info->hyphen_name);
}

/**
 * LteBusObjectProxy:
 *
 * The #LteBusObjectProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * LteBusObjectProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #LteBusObjectProxy.
 */

static void
lte_bus_object_proxy__lte_bus_object_iface_init (LteBusObjectIface *iface G_GNUC_UNUSED)
{
}

static void
lte_bus_object_proxy__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = lte_bus_object_notify;
  iface->interface_removed = lte_bus_object_notify;
}


G_DEFINE_TYPE_WITH_CODE (LteBusObjectProxy, lte_bus_object_proxy, G_TYPE_DBUS_OBJECT_PROXY,
                         G_IMPLEMENT_INTERFACE (LTE_BUS_TYPE_OBJECT, lte_bus_object_proxy__lte_bus_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, lte_bus_object_proxy__g_dbus_object_iface_init));

static void
lte_bus_object_proxy_init (LteBusObjectProxy *object G_GNUC_UNUSED)
{
}

static void
lte_bus_object_proxy_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value G_GNUC_UNUSED,
  GParamSpec   *pspec)
{
  G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
}

static void
lte_bus_object_proxy_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  LteBusObjectProxy *object = LTE_BUS_OBJECT_PROXY (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "dev.middleware.lte");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
lte_bus_object_proxy_class_init (LteBusObjectProxyClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = lte_bus_object_proxy_set_property;
  gobject_class->get_property = lte_bus_object_proxy_get_property;

  g_object_class_override_property (gobject_class, 1, "dev-middleware-lte");
}

/**
 * lte_bus_object_proxy_new:
 * @connection: A #GDBusConnection.
 * @object_path: An object path.
 *
 * Creates a new proxy object.
 *
 * Returns: (transfer full): The proxy object.
 */
LteBusObjectProxy *
lte_bus_object_proxy_new (GDBusConnection *connection,
  const gchar *object_path)
{
  g_return_val_if_fail (G_IS_DBUS_CONNECTION (connection), NULL);
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return LTE_BUS_OBJECT_PROXY (g_object_new (LTE_BUS_TYPE_OBJECT_PROXY, "g-connection", connection, "g-object-path", object_path, NULL));
}

/**
 * LteBusObjectSkeleton:
 *
 * The #LteBusObjectSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * LteBusObjectSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #LteBusObjectSkeleton.
 */

static void
lte_bus_object_skeleton__lte_bus_object_iface_init (LteBusObjectIface *iface G_GNUC_UNUSED)
{
}


static void
lte_bus_object_skeleton__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = lte_bus_object_notify;
  iface->interface_removed = lte_bus_object_notify;
}

G_DEFINE_TYPE_WITH_CODE (LteBusObjectSkeleton, lte_bus_object_skeleton, G_TYPE_DBUS_OBJECT_SKELETON,
                         G_IMPLEMENT_INTERFACE (LTE_BUS_TYPE_OBJECT, lte_bus_object_skeleton__lte_bus_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, lte_bus_object_skeleton__g_dbus_object_iface_init));

static void
lte_bus_object_skeleton_init (LteBusObjectSkeleton *object G_GNUC_UNUSED)
{
}

static void
lte_bus_object_skeleton_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  LteBusObjectSkeleton *object = LTE_BUS_OBJECT_SKELETON (gobject);
  GDBusInterfaceSkeleton *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (LTE_BUS_IS_DEV_MIDDLEWARE_LTE (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "dev.middleware.lte");
        }
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
lte_bus_object_skeleton_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  LteBusObjectSkeleton *object = LTE_BUS_OBJECT_SKELETON (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "dev.middleware.lte");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
lte_bus_object_skeleton_class_init (LteBusObjectSkeletonClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = lte_bus_object_skeleton_set_property;
  gobject_class->get_property = lte_bus_object_skeleton_get_property;

  g_object_class_override_property (gobject_class, 1, "dev-middleware-lte");
}

/**
 * lte_bus_object_skeleton_new:
 * @object_path: An object path.
 *
 * Creates a new skeleton object.
 *
 * Returns: (transfer full): The skeleton object.
 */
LteBusObjectSkeleton *
lte_bus_object_skeleton_new (const gchar *object_path)
{
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return LTE_BUS_OBJECT_SKELETON (g_object_new (LTE_BUS_TYPE_OBJECT_SKELETON, "g-object-path", object_path, NULL));
}

/**
 * lte_bus_object_skeleton_set_dev_middleware_lte:
 * @object: A #LteBusObjectSkeleton.
 * @interface_: (allow-none): A #LteBusDevMiddlewareLte or %NULL to clear the interface.
 *
 * Sets the #LteBusDevMiddlewareLte instance for the D-Bus interface <link linkend="gdbus-interface-dev-middleware-lte.top_of_page">dev.middleware.lte</link> on @object.
 */
void lte_bus_object_skeleton_set_dev_middleware_lte (LteBusObjectSkeleton *object, LteBusDevMiddlewareLte *interface_)
{
  g_object_set (G_OBJECT (object), "dev-middleware-lte", interface_, NULL);
}


/* ------------------------------------------------------------------------
 * Code for ObjectManager client
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:LteBusObjectManagerClient
 * @title: LteBusObjectManagerClient
 * @short_description: Generated GDBusObjectManagerClient type
 *
 * This section contains a #GDBusObjectManagerClient that uses lte_bus_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc.
 */

/**
 * LteBusObjectManagerClient:
 *
 * The #LteBusObjectManagerClient structure contains only private data and should only be accessed using the provided API.
 */

/**
 * LteBusObjectManagerClientClass:
 * @parent_class: The parent class.
 *
 * Class structure for #LteBusObjectManagerClient.
 */

G_DEFINE_TYPE (LteBusObjectManagerClient, lte_bus_object_manager_client, G_TYPE_DBUS_OBJECT_MANAGER_CLIENT);

static void
lte_bus_object_manager_client_init (LteBusObjectManagerClient *manager G_GNUC_UNUSED)
{
}

static void
lte_bus_object_manager_client_class_init (LteBusObjectManagerClientClass *klass G_GNUC_UNUSED)
{
}

/**
 * lte_bus_object_manager_client_get_proxy_type:
 * @manager: A #GDBusObjectManagerClient.
 * @object_path: The object path of the remote object (unused).
 * @interface_name: (allow-none): Interface name of the remote object or %NULL to get the object proxy #GType.
 * @user_data: User data (unused).
 *
 * A #GDBusProxyTypeFunc that maps @interface_name to the generated #GDBusObjectProxy<!-- -->- and #GDBusProxy<!-- -->-derived types.
 *
 * Returns: A #GDBusProxy<!-- -->-derived #GType if @interface_name is not %NULL, otherwise the #GType for #LteBusObjectProxy.
 */
GType
lte_bus_object_manager_client_get_proxy_type (GDBusObjectManagerClient *manager G_GNUC_UNUSED, const gchar *object_path G_GNUC_UNUSED, const gchar *interface_name, gpointer user_data G_GNUC_UNUSED)
{
  static gsize once_init_value = 0;
  static GHashTable *lookup_hash;
  GType ret;

  if (interface_name == NULL)
    return LTE_BUS_TYPE_OBJECT_PROXY;
  if (g_once_init_enter (&once_init_value))
    {
      lookup_hash = g_hash_table_new (g_str_hash, g_str_equal);
      g_hash_table_insert (lookup_hash, (gpointer) "dev.middleware.lte", GSIZE_TO_POINTER (LTE_BUS_TYPE_DEV_MIDDLEWARE_LTE_PROXY));
      g_once_init_leave (&once_init_value, 1);
    }
  ret = (GType) GPOINTER_TO_SIZE (g_hash_table_lookup (lookup_hash, interface_name));
  if (ret == (GType) 0)
    ret = G_TYPE_DBUS_PROXY;
  return ret;
}

/**
 * lte_bus_object_manager_client_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates #GDBusObjectManagerClient using lte_bus_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call lte_bus_object_manager_client_new_finish() to get the result of the operation.
 *
 * See lte_bus_object_manager_client_new_sync() for the synchronous, blocking version of this constructor.
 */
void
lte_bus_object_manager_client_new (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (LTE_BUS_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", lte_bus_object_manager_client_get_proxy_type, NULL);
}

/**
 * lte_bus_object_manager_client_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to lte_bus_object_manager_client_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with lte_bus_object_manager_client_new().
 *
 * Returns: (transfer full) (type LteBusObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
lte_bus_object_manager_client_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * lte_bus_object_manager_client_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates #GDBusObjectManagerClient using lte_bus_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See lte_bus_object_manager_client_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type LteBusObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
lte_bus_object_manager_client_new_sync (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (LTE_BUS_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", lte_bus_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


/**
 * lte_bus_object_manager_client_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like lte_bus_object_manager_client_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call lte_bus_object_manager_client_new_for_bus_finish() to get the result of the operation.
 *
 * See lte_bus_object_manager_client_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
lte_bus_object_manager_client_new_for_bus (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (LTE_BUS_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", lte_bus_object_manager_client_get_proxy_type, NULL);
}

/**
 * lte_bus_object_manager_client_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to lte_bus_object_manager_client_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with lte_bus_object_manager_client_new_for_bus().
 *
 * Returns: (transfer full) (type LteBusObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
lte_bus_object_manager_client_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * lte_bus_object_manager_client_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like lte_bus_object_manager_client_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See lte_bus_object_manager_client_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type LteBusObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
lte_bus_object_manager_client_new_for_bus_sync (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (LTE_BUS_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", lte_bus_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


